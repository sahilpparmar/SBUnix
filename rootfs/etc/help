---------------------------------- H E L P -----------------------------------
ps              process status
ls [directory]  list directory contents
cd              Change Directory
echo $PATH      displays the current value set in the PATH variable
export PATH     export can be used to set a custom value to PATH variable. 
                example: export PATH=/rootfs/bin
sleep {seconds} Puts the shell to sleep for specified numebr of seconds. 
                example: sleep 20
pwd             displays current working directory
ulimit          displays system limits
cat [file path] prints file contents, absolute file path to be given as input
                example: cat /absolute/path/file
sh {filepath}   execute shebang files. example: sh /path/shebang/file
./{command}     execute binaries using ./   example:  ./ps  ./ls
/path           execute binaries by entering the complete path.
                example: /rootfs/bin/ls
process&         strarts the process in background.  
                example: sleep 20 &

